{
  "name": "@goaly/plugin-onchain",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "On-chain reads/writes for Goaly (viem) — GoalyVault positions, addresses, ABIs",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "test:unit": "bun test",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@goaly/core": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
