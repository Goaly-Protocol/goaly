{
  "name": "@goaly/plugin-teams",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Team metadata resolver — maps team names to FIFA codes + flags/logos (no extra API calls)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "test:unit": "bun test",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
